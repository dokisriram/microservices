// which service it is 
describe("CustomerService", () => {
    // which function we are testing for sign in
    describe("SignIn", () => {
        // which scneario we are testing
        test("validate user inputs", () => {
            
        })

        test("validate response", async() => {

        })
    })
})